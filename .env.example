# PostgreSQL Database URL
# Format: postgres://username:password@localhost:5432/database_name
# For local development with default PostgreSQL:
DATABASE_URL=postgres://username@localhost:5432/sui_indexer

# Optional: Sui Indexer Configuration
# Checkpoint store URL (defaults to testnet)
# CHECKPOINT_STORE_URL=https://checkpoints.testnet.sui.io

# Optional: Starting checkpoint sequence number
# FIRST_CHECKPOINT=0

# Optional: Remote store URL for checkpoint data
# REMOTE_STORE_URL=https://checkpoints.testnet.sui.io
